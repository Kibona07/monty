#include "monty.h"

/**
 * stack_func - prints the top
 * @top: stack head
 * @count: line_number
 * Return: no return
*/

void stack_func(stack_t **top, unsigned int count)
{
	(void)top;
	(void)count;
	bus.lifi = 0;
}
